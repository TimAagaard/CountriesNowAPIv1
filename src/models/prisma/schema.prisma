datasource db {
    url         = "file:../../data/data.db?connection_limit=1&socket_timeout=10"
    provider    = "sqlite"
}

generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["prismaSchemaFolder"]
}